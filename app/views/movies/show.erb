<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Show each movie</title>
  </head>
  <body>
    <h2><%= @movies.first["title"] %> (<%= @movies.first["rating"] %>)</h2>
      <h3> (<%= @movies.first["studio"] %> <%= @movies.first["year"] %>) <%= @movies.first["genre"] %></h3>
    <ol>

    <% @movies.each do |info| %>

        <li> <%= info["character"] %>: <a href="/actors/<%= info["actor_id"] %>"><%= info["actor_name"] %></a> </li>
        <br>
    <% end %>
    </ol>

  </body>
</html>
